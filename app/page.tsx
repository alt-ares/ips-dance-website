"use client";

import { LoadingScreen } from "@/components/LoadingScreen";

export default function LanguageSelectionPage() {
  // Page de sélection de langue
  // Le noindex est géré dans le layout racine (app/layout.tsx)
  return <LoadingScreen />;
}
